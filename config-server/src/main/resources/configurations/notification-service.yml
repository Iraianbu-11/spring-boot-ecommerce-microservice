spring:
  data:
    mongodb:
      username: iraianbu
      password: password
      authentication-database: admin
      database: notification
      port: 27017
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.iraianbu.orderservice.kafka.OrderConfirmation,paymentConfirmation:com.iraianbu.paymentservice.body.PaymentNotificationRequest;
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer

  mail:
    host: localhost
    port: 1025
    username: iraianbu
    password: password
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

server:
  port: 8040